{
  "name": "spilli-web-scraper-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Installable Spilli external agent for Brave-backed web research and Playwright-rendered scraping.",
  "main": "agentLoop.js",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "smoke": "node -e \"require('./agentLoop.js'); console.log('runtime-load-ok')\"",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.53.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
